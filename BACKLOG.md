# AXIOM — бэклог (доработки, не блокируют запуск рассылки)

Приоритеты внутри бэклога: FIXME = сделать следующим после TODO.md, TODO = после этого, NOTE = справочно/идея на будущее.

## Backlog-P0 — МОДУЛЬ ДОСЬЕ (парсер чатов → люди → психографика) ⭐ большая задача

Идея Василия (2026-07-12): собирать не только тех.данные, а психо-портрет человека, чтобы бот писал «под его антропометрию».

Ревизия 2026-07-14: бэкенд-скелет модуля УЖЕ построен и работает (не был отмечен в бэклоге):
`channels/chat_scan.py` + `chat_inventory.py` (каталог чатов, «Ресурсы→Чаты»), `tg_parser.py --harvest`
(сбор сообщений в `tg_user_posts`), `agent/enrich_person.py` (Claude structured output → психо-портрет
в карточке контакта), `/api/target_leads` + карточка-досье в drawer'е контакта (score/боли/страхи/
желания/цитаты/рекомендованное сообщение/фото-анализ). Т.е. «список чатов + карточки людей» —
ЕСТЬ, просто без явного пункта меню «Досье» (сейчас разнесено между «Чаты», «Лиды», «Запросы»).
Сделано (сессия 2026-07-14, круг 1): карточка чата → «мои агенты в этом чате» (tg-ссылка + карточка
в AXIOM, через `account_chats`); карточка человека → блок «найден в чате» (название+ссылка+переход
на карточку чата) + поля **пол** (эвристика по имени, `channels/ru_names.gender_of`, теперь также
суффиксная эвристика для имён вне пула персон) и **Premium** (из Telethon `User.premium`, только
парсер, не harvest chat_hits). Заполняются при находке через `tg_parser` (парсинг) и `hit_to_lead`
(из «Запросов»); для уже существующих контактов — пусто, авто-бэкфилл не делали (перезапусти парсер
по нужным чатам).

Сделано (сессия 2026-07-14, круг 2) — устранён скрытый баг связки чатов + карточка чата по ТЗ:
- ⚠️ БАГ (жил в коде до нас): `chat_hits.chat_id` заполнялся НЕСОГЛАСОВАННО — `chat_keywords.py`
  клал каталожный `chats.id`, а `tg_listener.py` — сырой telegram-id. Плюс `tg_user_posts.chat_id` —
  всегда telegram-id. Из-за этого JOIN'ы на `chats` местами не находили чат (пропадали ссылки).
- Введено единое поле `chats.tg_chat_id` (сырой entity.id) + `database.resolve_catalog_chat()`
  (telegram-id → каталожный id, с авто-заведением чата). Все писатели выровнены: `chat_scan`,
  `chat_inventory`, `tg_listener`, `tg_parser --harvest` теперь пишут `tg_chat_id`; листенер
  резолвит в каталог перед записью хита. Исправлен вчерашний JOIN в `/api/contact/{id}`.
- Карточка ЧАТА (по ТЗ П0): новые поля `members_access` (открыт/скрыт/ограничен/закрыт/неизвестно) и
  `can_export_all` (да/частично>10k/нет) — считает `chat_scan.members_access()`; показаны в drawer'е.
Сделано (сессия 2026-07-14, круг 3) — AI-обогащение описания чата (пункт ТЗ «обогатить описание
чата: что это за чат, тематика»):
- Новый модуль `agent/enrich_chat.py` (Claude structured output `ChatProfile`: topic/summary/city/
  lead_fit) на runtime-модели (Haiku). Встроен в `chat_scan.run` — «📊 Анализ (чтение)» теперь за
  один проход собирает и активность, и выборку сообщений, и AI-тему/описание (без ключа — тихо
  пропускается). Колонки `chats.summary` + `chats.enriched_at`; описание показано в карточке чата,
  topic/city заполняются только если оператор не задал вручную.

Сделано (сессия 2026-07-14, круг 4) — фото человека в карточке:
- Аватар теперь качается для ВСЕХ активных лидов (не только `--harvest`) — `tg_parser._download_avatar()`
  в `data/avatars/{tg_user_id}.jpg` (тот же файл, что читает vision `enrich_person`). Колонка
  `contacts.has_photo` + `database.mark_photos_by_tg()`.
- Эндпоинт `GET /api/contact/{id}/photo` (FileResponse; 404 → плейсхолдер). `has_photo` в
  `contact_detail` авторитетно по файлу; добавлено в `target_leads`.
- UI: фото-кружок в шапке досье + миниатюра в карточках «Целевых лидов». Проверено TestClient'ом
  (404/200/has_photo). Телефон человека НЕ собираем — в TG почти всегда скрыт приватностью (по ТЗ
  «редко»), отдельный шаг не оправдан.

Сделано (сессия 2026-07-14, круг 5) — авто-поиск чатов по нише (модуль №1 Дениса «система сама
находит чаты»):
- Новый модуль `channels/chat_discover.py`: по ключам ниши (или явному запросу) прогоняет
  `SearchRequest` с антибан-паузами, заносит найденные ПУБЛИЧНЫЕ группы/каналы в каталог
  (статус 'new', с tg_chat_id, дедуп по tg_chat_id/username). Фильтры `--min-members`,
  `--groups-only`. Только поиск+запись, без вступления/рассылки.
- Эндпоинт `POST /api/chatcat/discover` (синхронно через _run_capture, парсит JSON-сводку).
- UI: панель «🔍 Авто-поиск чатов» в каталоге — выбор ниши / свой запрос / мин.участников /
  только группы. Проверено (argparse + эндпоинт 400 на пустой ввод).

- НЕ сделано (следующий круг): bio-скан ссылок и размножение по похожим чатам (модуль №5 Дениса —
  «достаёт спрятанные ссылки на закрытые группы, находит 1000 похожих»); авто-вступление найденных
  чатов сразу из discover (пока раздельно — «Разослать армию»); бэкфилл `tg_chat_id`/фото для старых
  записей (заполнится при повторном анализе/парсинге); мульти-модельность (DeepSeek/Gemini).

Пайплайн:
- FIXME: раздел «Досье» в «Ресурсах» пульта: список чатов + карточки людей.
- Вход: Василий подгружает ссылки на чаты/группы (публичные ИЛИ приватные +hash / /c/). Пример списка: `axiom/data/parse_targets_2026-07-12.txt`.
- Парсер по каждому чату:
  - зайти/проверить чат, **обогатить описание чата** (что это за чат, тематика).
  - в таблицу по чату: название/ссылка, **кол-во участников**, тип (список **открыт/скрыт/ограничен/закрыт**), можно ли выгрузить участников целиком, сколько собрано.
  - открытый список → GetParticipants (лимит ~10k). Скрытый/закрытый → НЕ лезть в список, а собирать по **комментаторам за последний год** (iter_messages, кто пишет).
- По каждому человеку: username, имя, **телефон** (если приватность позволяет — редко), фото.
- ДОСЬЕ (главная фишка): по его сообщениям в чатах «проиграть» переписку → карта личности: **страхи, желания, боли, запросы, скоринг/рейтинг** (Claude structured output) → чтобы бот понял, кто это, и написал персонально.

Модель данных (по образцу OPUS-карточек, скрины от Василия 2026-07-12) — расширить существующую CRM (Базы, 36811 контактов уже есть: score/сегмент/пол/район/теги):
- КАРТОЧКА ЧЕЛОВЕКА:
  - Идентификация: score (0-100) + метка hot/warm/cold, @username, имя, user_id, Premium (да/нет), телефон (если доступен), **пол, город/местонахождение**.
  - Источник: **в каком чате найден** (название) + chat_id + **ссылка на чат**, метод (напр. hyperlocal_active), test_run, создан/обновлён.
  - Намерение (Intent): summary («ищет двушку в Мамайке, бюджет до 30 млн»), тип, район, бюджет мин/макс, срочность.
  - Bio из Telegram (если получено).
  - Score Breakdown (из чего сложился скоринг).
- КАРТОЧКА ЧАТА: при клике — **сколько МОИХ агентов сидит в этом чате**, со ссылками: tg-ссылка на агента + ссылка на его карточку в AXIOM. Плюс поля чата (участники, тип списка, тематика — см. выше).
- Сбор базы по профилю идеального клиента: пол, возраст, город, интересы, платёжеспособность → автосегментация + скоринг.

Заметки по реализации:
- Переиспользовать: tg_parser (поиск/админы/участники/активные), tg_listener (по ключам), enrich (см. ygroup-scraper), существующую CRM/Базы (пол/район/score/сегменты уже есть). Скоринг лидов — приоритет №1 из opus-parity-backlog.
- ⚠️ Психографика + Intent требуют РАБОЧИЙ ANTHROPIC_API_KEY — сейчас в .env заглушка «ваш_ключ» (блокер). У Василия на console.anthropic $5 кредита, лимит $500.
- МУЛЬТИ-МОДЕЛЬНОСТЬ (идея Василия, Денис так и делает): выбирать модель под задачу и платить за токены. Массовое обогащение/досье/веб-скан/слушание чатов — САМОЙ ДЕШЁВОЙ моделью (DeepSeek-V4-Flash ~$0.14/$0.28 за 1М, Gemini Flash-Lite, Haiku), дорогой Opus — только где нужно качество/сложный диалог. Реализация: слой-абстракция провайдера (Anthropic + OpenAI-совместимые: DeepSeek/Gemini), в настройках агента/задачи выбор модели. Денис: «клод-код = терминал, мозги любые».
- Телефон в TG виден редко (приватность) — в основном username/name/photo.
- Развить «глазок»: у аккаунтов — лог действий (упаковка/прогрев: что делал, когда, успех/ошибка); упаковка уже пишет per-account события (сделано 2026-07-12). У чатов/людей — карточка досье.

## Backlog-P1 — авторизация пульта

- ✅ СДЕЛАНО (2026-07-14): вход по паролю — `AXIOM_PASSWORD` в `.env` включает гейт (cookie+HMAC,
  `web/app.py` middleware `_auth_gate` + `/login`). Пусто = пульт открыт (локальный режим).
  ⚠️ Ещё не закоммичено в git — было готово в рабочей копии, не отмечено в бэклоге. Задай пароль
  на сервере и закоммить перед реальным запуском!
  ⚠️ Реально безопасно только под HTTPS (иначе пароль идёт открытым текстом) — сделать дальше.
- TODO: Telegram Login Widget — ждём от Василия: токен + username нового бота из @BotFather, домен внешнего сервера
- NOTE: один пользователь (без ролей/команды) — устраивает, не делать сложнее

## Backlog-P2 — личный бот-ассистент (Toki-стиль)

- TODO: личный Telegram-бот "AXIOM Assistant" — голос/текст → событие в Google Calendar
  - движок уже есть: integrations/calendar.py (create_event), integrations/meetings.py (parse_slot) — переиспользовать, не писать заново
  - текст → структура события: через Claude (structured output), не regex-парсинг
  - голос → текст: OpenAI Whisper API (нужен OPENAI_API_KEY, ~$0.006/мин) — решено, локальный Whisper как запасной вариант на будущее
- TODO: в этом же боте — команда "добавить клиента из переписки" → создание записи в contacts + pipeline (AXIOM CRM), не только календарь

## Backlog-P3 — прочее / идеи

- NOTE: WhatsApp-канал (Baileys) как второй канал рассылки — см. также TODO.md
