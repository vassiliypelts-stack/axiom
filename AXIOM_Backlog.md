# AXIOM — бэклог желаемого (v1, 2026-06-17)

> Сырые идеи Василия, разложенные по темам. Для каждой: **суть**, **связь с кодом**
> (что уже есть), **зависимости**, **фаза**, **открытые вопросы**.
> Это не план, а карта хотелок — приоритеты расставляем отдельно.
> Спека пилота: [AXIOM_Pilot.md](AXIOM_Pilot.md).

Легенда фаз: **1.5** — расширение текущего пилота · **2** — продукт/SaaS, дашборд, мульти-агент.

---

## A. Источники базы и импорт

### A1. Импорт компаний из 2GIS (с TG/WhatsApp-ссылками)
- **Суть:** грузить список компаний из спарсенного 2GIS, где есть ссылки на их Telegram/WhatsApp, и сразу иметь возможность писать.
- **Связь с кодом:** уже есть [importer/import_2gis.py](axiom/importer/import_2gis.py) — тянет телефон, `@username`, `wa.me`, проставляет `has_tg/has_wa='yes'` из ссылок (бесплатный чекер). Запись — через готовый `channels/telegram.py` и `exporter/`.
- **Зависимости:** формат 2GIS-выгрузки фиксирован в `COL` — под другой источник нужен свой парсер.
- **Фаза:** 1.5 (почти готово).
- **Открытые вопросы:** какие ещё источники кроме 2GIS (Avito? Яндекс? выгрузки бирж)? Нужен ли единый «адаптер импорта» с маппингом колонок?

### A2. Интеграция с Google Contacts
- **Суть:** подтягивать контакты из личной адресной книги Google.
- **Связь с кодом:** новый `importer/google_contacts.py`; переиспользует OAuth-механику из [integrations/calendar.py](axiom/integrations/calendar.py).
- **Зависимости:** People API + scope `contacts.readonly`.
- **Фаза:** 1.5.

---

## B. Человекоподобный диалог

### B1. Диалог по частям с паузами (drip), а не «портянкой»
- **Суть:** не одно длинное сообщение, а несколько коротких поочерёдно: «Здравствуй, Василий» → пауза → «как дела, давно не виделись» → пауза → «я тут занимаюсь тем-то». Имитация живого набора.
- **Связь с кодом:** агент уже возвращает текст; `channels/telegram.py` уже шлёт с человеческой задержкой (`REPLY_DELAY`) и ловит «следы бота» в `agent/sales_agent_prompt.md`.
- **Что добавить:** агент должен возвращать **массив реплик** (`reply_parts: list[str]`), а адаптер — слать их по очереди с паузами и индикатором «печатает…» (`client.action(..., 'typing')`).
- **Зависимости:** правка схемы вывода `Reply` в [agent/agent.py](axiom/agent/agent.py) + цикл отправки.
- **Фаза:** 1.5 (высокий приоритет — прямо влияет на конверсию и антибан).

---

## C. Модуль слушания чатов (lead-listening)

### C1. Мониторинг 1000+ чатов по ключевым словам
- **Суть:** агенты состоят в множестве чатов и отслеживают, где люди оставляют заявки по ключевым словам («ищу квартиру», «новостройка», «коммерческий объект», «инвестиции», «куплю/сниму»). Найденного — в работу как потенциального клиента.
- **Связь с кодом:** новый `channels/listener.py` — отдельный `events.NewMessage` хендлер на чаты (не на личку); найденное пишет в `contacts` с `source='chat'` (в схеме уже предусмотрено) и `tags`.
- **Зависимости:** аккаунт состоит в чатах; настраиваемый список ключевых слов/ниш; антифлуд; дедуп людей.
- **Фаза:** 2 (в спеке это «парсинг чатов», отложено). Высокая ценность.
- **Открытые вопросы:** какие ниши/ключи? Писать в ЛС сразу или копить в «входящие лиды» на ревью? Риск бана за ЛС незнакомцам — выше, чем по тёплой базе.

### C2. Каталог чатов + проверка жизнеспособности
- **Суть:** в этом же разделе — каталог всех чатов, где я состою, с проверкой на актуальность/живость (активен ли чат, сколько участников, есть ли движение). Плюс Telegram-парсер чатов (поиск чатов по теме/ключам, сбор участников).
- **Связь с кодом:** новый `channels/chats.py` + таблица `chats` (id, title, type, members, last_activity, alive, joined). Парсер через Telethon (`GetDialogs`, поиск, выгрузка участников — баноопасно, осторожно).
- **Зависимости:** аккаунт(ы) в чатах; антифлуд; реестр аккаунтов (G2).
- **Фаза:** 2.
- **Открытые вопросы:** парсить участников (рискованно) или только мониторить сообщения? Как мерить «живость» — сообщений/день?

### C3. Вступление агентов в чаты + постинг/активность
- **Суть:** агенты-слушатели вступают в нужные чаты; по команде постят общий пост / рекламу / вопрос, проявляют активность (чтобы выглядеть живыми и для охвата).
- **Связь с кодом:** действия Telethon `JoinChannel`/`send_message` в чат; шаблоны постов; расписание активности с рандомом (пересекается с прогревом G1b).
- **Зависимости:** C2 (каталог), G1a (лимиты/антибан), G2 (аккаунты).
- **Фаза:** 2. ⚠️ постинг/реклама в чужие чаты — высокий риск бана и жалоб, нужны строгие лимиты и правила.
- **Открытые вопросы:** только свои чаты или чужие? Частота постинга на аккаунт? Как не словить бан за рекламу?

---

## D. Назначение встреч — гибкие варианты

### D1. Постоянная ссылка Zoom/Телемост + выбор слота
- **Суть:** агент предлагает на выбор: постоянная ссылка (Zoom/Яндекс Телемост) ИЛИ ссылка на свободные слоты в Google Calendar, чтобы человек сам выбрал время. Спрашивает «как удобнее: так или так».
- **Связь с кодом:** [integrations/meetings.py](axiom/integrations/meetings.py) уже создаёт Zoom под слот. Добавить: режим «постоянная ссылка» (из конфига) и режим «букинг-ссылка».
- **Зависимости:** конфиг `PERMANENT_MEETING_URL`; для самостоятельного выбора слота — публичная страница записи.
- **Фаза:** 1.5.

### D2. Двусторонняя синхронизация с личным Google Calendar
- **Суть:** события создаются и **синхронизируются** с личным календарём; агент видит занятость и предлагает реально свободные слоты.
- **Связь с кодом:** [integrations/calendar.py](axiom/integrations/calendar.py) уже пишет события; добавить `freebusy.query` для подбора свободных слотов (сейчас слоты — заглушка `_default_slots` в `channels/telegram.py`).
- **Фаза:** 1.5 → 2.
- **Открытые вопросы:** какой календарь — основной личный? Рабочие часы/буферы между встречами?

---

## E. Задачи дня (TODO по коммуникациям)

### E1. Список задач на день + кастомные кампании
- **Суть:** ежедневный список задач по коммуникации (кому написать/дожать/напомнить), который можно настраивать. «Искусственный продавец», которого можно ставить на конкретные задачи.
- **Связь с кодом:** `scheduler.collect_due()` уже вычисляет, кого пора дожать/напомнить — это и есть ядро TODO-листа. Добавить представление «задачи на сегодня» (выборка из книжки) + статус выполнения.
- **Фаза:** 1.5 (бэкенд почти есть) → визуал в фазе 2.

---

## F. Кампании под продукт (сегментированный аутрич)

### F1. Кампания: выбрать N людей по критериям → персональный заход → вывод на КЭВ
- **Суть:** «у меня есть продукт» → выбрать из базы 3/20/50/100 человек по критериям (с оценкой/скорингом) → связаться человекоподобно («привет, Василий, вы раньше интересовались X, у меня как раз новый продукт, интересно?») → кто откликнулся, того на созвон/КЭВ. Настраивается под конкретную компанию/продукт.
- **Связь с кодом:** объединяет почти всё готовое — выборка из `contacts` (фильтры по `tags/city/status`), `agent/` (заход под оффер), `channels/telegram.py` (отправка), `scheduler.py` (дожим). Не хватает «сущности кампании»: оффер + сегмент + шаблон захода + метрики.
- **Что добавить:** таблица `campaigns` (id, name, offer, segment_filter, status) и привязка `deals/messages` к кампании; скоринг контактов.
- **Фаза:** 2 (это уже product-уровень, ядро под себя — фаза 1).
- **Открытые вопросы:** по каким критериям скоринг? Один аккаунт на кампанию или пул?

---

## G. Интерфейс / дашборд (разделы бокового меню)

> В спеке интерфейс заложен как **NocoDB поверх SQLite** (быстрый старт), свой дашборд — фаза 2.
> Ниже — желаемые разделы. На старте каждый = вид/таблица в NocoDB; позже — кастомный UI.

### G1. Command Dashboard (верхний раздел)
- Сводная статистика, аналитика, визуалы: отправлено/ответы/встречи/дожимы, конверсии воронки, по дням.
- **Связь с кодом:** всё считается из `contacts/messages/deals`. **Фаза:** 2.

### G1a. Антибан: лимиты и распределение по аккаунтам
- **Суть:** команда из ~20 TG + 10 WA аккаунтов. Хочу задавать лимиты галочками: «сегодня 100 людей, по 5 на аккаунт», система распределяет нагрузку по живым аккаунтам и шлёт с рандомными паузами (от-до сек), чтобы Telegram/WhatsApp не заподозрили слишком интенсивное общение.
- **Связь с кодом:** база есть — `config.DAILY_FIRST_MESSAGES` (лимит/аккаунт) и рандомные паузы `OUTREACH_PAUSE` в [channels/telegram.py](axiom/channels/telegram.py). Сейчас это один аккаунт; нужно: пул аккаунтов (таблица `accounts`, см. G2), раскладка задания по аккаунтам с учётом лимита и «здоровья», окно активности по времени суток.
- **Зависимости:** G2 (реестр аккаунтов), мульти-сессии (несколько StringSession), по прокси на аккаунт.
- **Фаза:** 2. 🔥 ключевое для масштаба и выживания аккаунтов.
- **Открытые вопросы:** лимиты раздельно для холодных/тёплых? Дневное окно (напр. 10:00–21:00)? Что делать при FloodWait — пауза аккаунта или переброс на другой?

### G1b. Прогрев аккаунтов (warming)
- **Суть:** как в WaCombo — загружаешь 5-10 аккаунтов, они 1-2 недели переписываются между собой по шаблонам, человекоподобно (днём активность, паузы, рандом), чтобы выглядеть живыми для антиспама перед боевой работой.
- **Связь с кодом:** новый `warming.py` — оркестратор: пары аккаунтов, шаблоны диалогов, расписание с рандомом в дневном окне, постепенный рост активности по дням.
- **Зависимости:** пул аккаунтов (G2), мульти-сессии.
- **Фаза:** 2. 🔥 без прогрева новые аккаунты быстро банятся.
- **Открытые вопросы:** только между своими или с «тёплой» внешней активностью (вступление в чаты, чтение)? Кривая роста (день 1: 2-3 сообщения → день 14: рабочий объём)?

### G2. Команда (аккаунты-агенты)
- **Суть:** таблица действующих аккаунтов: номер телефона, статус в TG (бан/жив), в WhatsApp (бан/жив), живой/мёртвый. Сортировка/фильтр/группировка. Видеть, сколько живых, и выбирать их при постановке задач на кампанию.
- **Связь с кодом:** новой сущности нет. Нужна таблица `accounts` (phone, tg_status, wa_status, proxy, session, alive, last_check). Чекер статуса — расширение [checker/checker.py](axiom/checker/checker.py) (он уже про «есть ли аккаунт по номеру», тут — про здоровье своих аккаунтов).
- **Фаза:** 2 (нужно для мульти-аккаунта; пилот — 1 аккаунт).
- **Открытые вопросы:** как детектить бан автоматически (попытка отправки/`GetState`)?

### G3. Чаты / переписки
- Просмотр диалогов с людьми. **Связь с кодом:** `messages` уже хранит всю переписку. **Фаза:** 2 (вид в NocoDB сразу).

### G4. Досье на человека
- **Суть:** карточка на каждого: кто, город, телефон, **боли/страхи/желания**, чтобы понимать тон и от какой боли заходить.
- **Связь с кодом:** базовые поля есть в `contacts`. Добавить досье-поля (pains, fears, desires, summary) — заполняет ИИ из истории диалога/публичных данных. Это «AI-досье» из спеки (отложено в фазу 2).
- **Фаза:** 2.
- **Открытые вопросы:** источники для досье (только диалог? публичные профили?) — приватность.

### G5. CRM с воронкой (лиды → стадии)
- **Суть:** канбан-воронка, разделение на лиды; загрузка телефонов из 2GIS или простого списка; подтягивание из адресной книги (Google Contacts → см. A2).
- **Связь с кодом:** `deals.stage` + `contacts.status` уже описывают воронку; NocoDB даёт канбан из коробки. Импорт — A1/A2.
- **Фаза:** 1.5 (канбан в NocoDB) → свой UI в фазе 2.

---

## H. Разбор материалов Дениса (ЛидКадр / OPUS) — gap-анализ

> Источник: 2 PDF Дениса Вернякова (ЛидКадр Pricing v3.0 + OPUS Guide) и расшифровка
> видеовстречи 22.04.2026. Задача: выявить, **чего у нас НЕТ** против того, что он показывает.
> Вывод: 70% его «фишек» у нас уже в коде. Реальных дыр — 3 (H1–H3), плюс хвост по UI (H4).
> Что Денис сам НЕ использует сторонних платформ — всё написано им с нуля через вайб-кодинг
> (Claude Code/Opus). NeuroAgents — это конкурент, которого он заменил, а не его инструмент.

> **РЕШЕНИЯ Василия (25.06.2026):**
> - **H1 — СТРОИМ.** Первый шаг — доработка парсера (тянуть сообщения человека из чатов).
> - **H2 — строим, но запуск позже:** когда аккаунтов станет больше; включать под задачу
>   через кампании (или иной логикой). По умолчанию — **оператору на обзор**; авто-ЛС —
>   опцией на будущее.
> - **H3 — нужен, но позже:** после запуска парсера и рабочей версии на VPS дотачиваем
>   (tool-use «не убежит»).
> - **H4 — TODO:** Василий уточнит требования к визуалу отдельно.

### Таблица: его фишка → что у нас есть → дыра
| Фишка Дениса | У нас сейчас | Дыра |
|---|---|---|
| Конвейер сбора: авто-поиск чатов по нише → вступление → парсинг всех участников | `tg_parser`, `chat_scan`, `chat_inventory`, `tg_invites`, авто-вступление (волна C) | 7+ способов «достать кого угодно» — bio-ссылки, скрытые/альтернативные ссылки, поиск похожих чатов по одной ссылке. У нас базовый парс |
| **AI-досье: боли/страхи/желания, психотип, портрет ЦА по сообщениям из чатов** | `enrich.py` — но это B2B-обогащение юрлиц (ЕГРЮЛ/DaData + текст сайта) | 🔴 **H1 — главная дыра.** Психо-портрет по реальным сообщениям человека из чатов мы НЕ строим |
| Мониторинг 600 чатов 600 аккаунтами в реальном времени по 5000 триггер-слов → авто-ЛС «из заботы» | `chat_keywords.py` — поллинг 1 аккаунтом, ключи ниш, находки → оператору на обзор | 🔴 **H2.** Нет: real-time listener-события, масштаб мультиаккаунт, авто-ЛС по триггеру |
| Агент с «78 инструментами» по категориям (комм-11, CRM, платежи, встречи, эскалация, БЗ) | `agent.py` возвращает структуру (intent/КП/встреча), но **реальных tool-call'ов у агента НЕТ** | 🔴 **H3.** Нет реестра инструментов-функций, которые агент сам вызывает |
| Скоринг лида AI + автосегментация по сферам | теги/статусы есть; числового score и автосегментации по сфере — нет | 🟡 часть H1 |
| Визитки контактов, имитация печати, кружки, стикеры, геолокация как действия агента | имитация печати + дробление реплик есть (`telegram.py`, `reply_parts`) | 🟡 остальное — часть H3 |
| Антибан, прогрев, премиум-аккаунты, прокси | `warmup.py`, `antiban.py`, `proxy_pool`, `health`, @SpamBot-чек | ✅ есть, дыр почти нет |
| Дожим/реактивация (3/7/30 дней) | `scheduler.py` (`collect_due`) | ✅ есть |
| Несколько КП под сегменты, отправка файлом | `kp_choice` в агенте + send_kp | ✅ есть |

---

### H1. 🔴 AI-портрет персоны (боли/страхи/желания) по сообщениям из чатов — ✅ СТРОИМ
- **Что у Дениса:** на выходе карточка — аватар, имя, город, сферы, теги, **всё что человек писал/думал в чатах**, темы, интересы, **боли**, сегментация, **скоринг AI**, рекомендация подхода («начни с вопроса про его опыт в маркетинге»), детект визитки-самопрезентации, кросс-соцсети. «Знаешь о нём больше, чем он сам помнит». Это он продаёт по 200 карточек за 30 000 ₽.
- **Эталон карточки (скрин 25.06, «Марина Фишер»)** — что должно быть в досье:
  - Шапка: **аватар, Имя | Роль | Город**, сегмент-подпись («Женщина-предприниматель · Сочи»), кнопка «Открыть в Telegram», @username, **QR-код** на профиль.
  - Специализация одной строкой + **«достоверность 85% по профилю»** — у каждого вывода **процент уверенности** (важно: не выдаём догадку за факт).
  - Саммари-абзац (кто, чем занимается, свежие факты — «недавно была вне Сочи»).
  - **Анализ фото/аватара:** «business casual · 35-50 · ring on left hand» — дресс-код, возрастная вилка, признаки статуса/семейного положения по картинке.
  - **Веб (не подтверждено):** обогащение из соцсетей/сайта с явной пометкой достоверности.
  - **Рекомендуемое сообщение:** готовый персональный крючок («Заметил, что вы организуете нетворкинги в Сочи»).
  - **Логика профайлинга и принятия решения** (запрос Василия): не только портрет, но и **как агент решает** — что этому человеку продавать и по какой траектории заходить (на основе болей/желаний/сегмента). Связка с H3 (траектории агента).
- **Что у нас:** [agent/enrich.py](axiom/agent/enrich.py) обогащает **юрлица** (агентства недвижимости): ЕГРЮЛ через DaData + текст сайта → person_name, specialization, hook, summary. Полей `pains/fears/desires/psychotype/interests/score/segment` **нет** ни в модели `Enrichment`, ни в схеме `contacts` ([db/database.py](axiom/db/database.py): только person_name, role, specialization, hook, bio, inn, ogrn).
- **Что добавить:**
  - **Доработка парсера (первый шаг, решено):** при парсинге участника тянуть его **последние N сообщений по чатам** (Денис — за 90 дней) + bio + аватар, складывать как сырьё для досье. Сейчас парсер берёт username/имя — этого мало.
  - Новый обогатитель `enrich_person.py` — на вход **реальные сообщения человека из чатов** (а не сайт), на выход психо-портрет: боли, страхи, желания, темы/интересы, стиль общения, оптимальное время, **рекомендуемое сообщение** (готовый крючок), числовой score, авто-сфера.
  - **Достоверность:** каждый вывод с % уверенности (`confidence`), факты не путать с догадками.
  - **Анализ аватара** (vision-модель): дресс-код, возрастная вилка, признаки статуса — с пометкой «по фото».
  - **Веб-обогащение** с флагом «не подтверждено» (соцсети/сайт).
  - Поля в `contacts`: `pains, fears, desires, interests, psychotype, comm_style, score, segment, quotes` (цитаты из чатов), `rec_message`, `photo_analysis`, `confidence`, `web_note`.
  - **Карточка-визитка:** рендер досье + QR на профиль (Денис продаёт по 200 шт за 30к — это готовый продукт «Разведка»).
  - (Опц.) детект визитки-самопрезентации в нетворкинг-чатах — фаза 2, аккуратно с приватностью.
- **Связь с волной F** (ROADMAP «профайлинг физлиц») — это она и есть, но конкретизированная.
- **Фаза:** 1.5→2. 🔥 это ядро ценности Дениса и прямой драйвер конверсии 15-20% vs 3%.

- **ПЛАН РЕАЛИЗАЦИИ (утв. 25.06.2026):** глубина **~30 сообщ./90 дней** · аватар-vision **потом** (этап 4) · модель **Haiku + Batch API**.
  - ✅ **Этап 1 (БД) — СДЕЛАНО:** таблица `tg_user_posts` (tg_user_id, chat_id, chat_title, text, msg_id, ts; дедуп tg_user_id+chat_id+msg_id) + поля в `contacts`: `pains, fears, desires, interests, psychotype, comm_style, best_time, score, segment, quotes, rec_message, photo_analysis, confidence, web_note` (миграция `_EXTRA_CONTACT_COLS`).
  - ✅ **Этап 2 (парсер) — СДЕЛАНО + протестировано вживую** (@biznesdvigsochi, собраны тексты+bio): флаг `--harvest` — **сохранять тексты** авторов (не только счётчик, см. [tg_parser.py:97-100](axiom/channels/tg_parser.py#L97-L100)), до ~30/чел за 90 дней → `tg_user_posts`; дотянуть `bio` (GetFullUser.about) с паузами; скачать аватар в `data/avatars/` (анализ — этап 4).
  - ✅ **Этап 3 (обогатитель) — СДЕЛАНО + протестировано вживую** (3 досье построено на Haiku, score/боли/желания/крючок корректны): `agent/enrich_person.py` — вход сообщения+bio, выход `PersonProfile` (боли/страхи/желания/интересы/психотип/стиль/время/сегмент/score/rec_message + `confidence` по блокам). Haiku, Batch API −50% (паттерн из `enrich.py`). Зачаток «логики решения» — поле «подход»; полные траектории → H3.
  - ✅ **Этап 4 (vision) — СДЕЛАНО + протестировано:** парсер качает аватар в `data/avatars/{id}.jpg`, `enrich_person` шлёт фото в Haiku-vision → `photo_analysis` (дресс-код/возраст/статус «по фото»), видно в карточке. Живая докачка на боевых аккаунтах — по мере выхода из FloodWait.
  - ✅ **Этап 5 (UI) — СДЕЛАНО:** в карточке лида блок «🧠 AI-досье (по чатам)» — score, достоверность %, сегмент, боли/страхи/желания/интересы, психотип/стиль/время, цитаты, рекомендуемое сообщение (index.html). Аватар-фото и QR — остаются на этап 4/позже.

### H2. 🔴 Мониторинг чатов в масштабе + авто-ЛС по триггеру — ⏳ строим, запуск позже
- **Что у Дениса:** 600 аккаунтов слушают 600 чатов, 5000 триггер-слов, в реальном времени; как только человек заикнулся («ищу квартиру по ипотеке») — агент сразу пишет в ЛС «из позиции заботы».
- **Что у нас:** [channels/chat_keywords.py](axiom/channels/chat_keywords.py) — **поллинг** (заходит и сканирует «раз в день») **одним** аккаунтом; ключи берутся из `niches`; находки → `chat_hits` оператору на ручной обзор. Есть `tg_listener.py` (события) — проверить, на что он сейчас навешан.
- **Что добавить:**
  - ✅ **Реал-тайм режим — СДЕЛАНО:** `tg_listener.py` переписан на воронку `niches → chat_hits` (как поллинг), мгновенная реакция через `events.NewMessage`, находки → «Запросы» оператору. Протестировано офлайн (ниша→матч→hit). Живой TG-тест — после FloodWait.
  - ⏳ Раскладка чатов по пулу аккаунтов (`antiban.py`/реестр) — масштаб 600 чатов. **Позже** (когда аккаунтов больше).
  - ⏳ Опция **авто-ЛС по триггеру** — флаг `--auto-dm` пока заглушка (только лог). Реальная отправка — осознанно, с лимитами. **Позже.**
  - Триггер-слова: сейчас простой `in` по строке — позже наборы на 1000+ ключей, нормализация, стоп-слова.
- **Фаза:** 2. 🔥 «скорость + автопоиск тех, кто нуждается здесь и сейчас» — его слова. Ядро (реал-тайм + обзор) готово; масштаб/авто-ЛС — по росту аккаунтов.

### H3. 🔴 Агент с реестром инструментов (его «78 tools») — ⏳ после запуска парсера/VPS
- **Что у Дениса:** агент сам вызывает инструменты по категориям — коммуникация (11: фото, видео-превью, документ, стикер, геолокация, визитка, имитация печати, задержка), CRM (сменить стадию, автотеги, скоринг, назначить менеджера), платежи (подписка, Telegram Stars, крипта), встречи (календарь/слоты), эскалация на человека, база знаний, карточка продукта. Модель-независимо (Opus/Haiku/DeepSeek под задачу).
- **Что у нас:** [agent/agent.py](axiom/agent/agent.py) **не делает tool-call'ов** — возвращает структуру `Reply` (intent, meeting_agreed, send_kp, kp_choice, notes), а действия (отправка, Zoom, КП) дёргает уже наш код снаружи. Это работает, но это не «агент с инструментами».
- **Что добавить (поэтапно, НЕ всё сразу):**
  - ✅ **Реестр инструментов — СДЕЛАНО (фундамент):** `agent/tools.py` — каталог по 6 категориям (communication/crm/meetings/payments/escalation/knowledge), 12 tools (4 рабочих: `crm_set_stage/add_tag/set_score`, `escalate_to_human` — пишут в БД; 8 заглушек). `anthropic_tools(categories)` отдаёт схемы для tool-use API, `dispatch()` исполняет. Протестировано офлайн. Рабочий `agent.py` НЕ тронут.
  - ⏳ Подключить tool-use в живой диалог (`generate_reply`) — **позже**, аккуратно, чтобы не сломать рабочего агента.
  - ⏳ Дореализовать заглушки: встречи (есть `meetings.py`), коммуникация (действия канала), платежи/Stars — по мере надобности.
  - Модель под задачу — уже есть `AGENT_MODEL` (Haiku для рантайма); расширить до выбора на кампанию.
- **Важно:** Денис строит это **сам с нуля**, готовой «библиотеки расширений под категории» как продукта НЕ существует — это его код агент-функций. Нам тоже строить своё, отдельную платформу покупать не нужно.
- **Фаза:** 2. Средний приоритет — текущая схема (структурный Reply) пилот закрывает.

### H4. 🟡 Визуал / UI (по твоей пометке «нужно ещё визуаль дать»)
- ✅ **Сделано (26.06.2026):** свёртываемое меню «как в Битрикс24» (заголовки групп с подсветкой + вращающийся шеврон, состояние помнится); **баннер-позиционирование вверху дашборда** — «AXIOM — нейро-отдел продаж в Telegram» (выбран угол «замена отдела продаж»).
- Карточка лида уровня Дениса: аватар, сферы-теги, **score крупно**, цитаты из чатов, «рекомендация подхода», кнопка «Написать».
- Сегментация по сферам с счётчиками (как на его скрине: IT 569 / бизнес 145 / маркетинг 51…) — у нас фильтры есть, довести вид.
- Это волна U в [ROADMAP](axiom/ROADMAP.md); конкретику по картинкам ты обещал одобрить отдельно.

### Открытые вопросы H — ✅ решены (25.06.2026)
1. ~~H1 — сбор сообщений человека из чатов как источник досье?~~ → **ДА**, доработка парсера — первый шаг.
2. ~~H2 — авто-ЛС по триггеру или только оператору?~~ → **по умолчанию оператору**; авто-ЛС опцией позже, запуск при росте аккаунтов, под кампанию.
3. ~~H3 — реальный tool-use в пилоте?~~ → **позже**, после парсера и рабочей версии на VPS.
4. **H4 — открыт:** Василий уточнит требования к визуалу карточки/UI.

---

## Сводка приоритетов (черновик — уточнить)

| Хочу | Фаза | Готовность | Эффект |
|---|---|---|---|
| B1. Диалог по частям с паузами | 1.5 | средняя (правка `Reply` + отправка) | 🔥 конверсия + антибан |
| A1. Импорт 2GIS + писать | 1.5 | почти готово | 🔥 наполнение базы |
| D1. Гибкий выбор слота / постоянная ссылка | 1.5 | средняя | удобство, меньше трений |
| E1. Задачи дня | 1.5 | бэкенд есть | контроль |
| F1. Кампании под продукт | 2 | нужна сущность `campaigns` | 🔥 масштаб |
| C1. Слушание чатов | 2 | новый модуль | 🔥 новый источник лидов |
| G1–G5. Дашборд (CRM/команда/досье/чаты/аналитика) | 2 | NocoDB сразу, UI позже | управляемость |

---

## Открытые вопросы для следующего шага
1. Что из 1.5 берём первым — **B1 (паузы)** как самый быстрый и влияющий на конверсию?
2. NocoDB разворачиваем сейчас (даст сразу CRM/чаты/команду таблицами) или ждём своего UI?
3. Слушание чатов (C1) — отдельный продукт или часть AXIOM?
